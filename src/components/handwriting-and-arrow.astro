<div class="decoration-text-arrow">
  <div>
    <h2 class="handwritten">Projects</h2>
    <slot />
    <svg
      class="svg-container"
      xmlns="http://www.w3.org/2000/svg"
      viewBox="0 0 70 106"
    >
      <path
        class="arrow-stem"
        d="M 0,0 C 20,10 40,30 25,60 C 13,80 25,95 34,100"></path>

      <polygon class="arrow-tip" points="34,96 40,104 30,102"></polygon>
    </svg>
  </div>
</div>

<style>
  @keyframes reveal {
    from {
      clip-path: polygon(0% 0%, 0% 100%, 0% 100%, 0% 0%);
    }
    to {
      clip-path: polygon(0% 0%, 0% 100%, 100% 100%, 100% 0%);
    }
  }

  .handwritten {
    font-family: var(--decoration-typeface-handwritten);
    font-size: calc(var(--step-6) * 1.3);
    color: var(--color-black);
    font-weight: 100;
    line-height: 1.2;

    @media (prefers-reduced-motion: no-preference) {
      & {
        animation: reveal 1.2s linear backwards 0.5s;
      }
    }
  }

  .decoration-text-arrow {
    position: absolute;
    bottom: 7%;
    left: 19%;
    rotate: -25deg;
    aspect-ratio: 1;
    user-select: none;

    & .svg-container {
      position: relative;
      left: 1.5rem;
      top: 0rem;
      width: 4rem;
      rotate: 25deg;
      fill: none;
      stroke: var(--color-black);
      stroke-width: 1px;
      stroke-linecap: round;
      stroke-linejoin: round;
    }
  }

  .arrow-tip {
    opacity: 1;
    fill: var(--color-black);
  }

  @media (prefers-reduced-motion: no-preference) {
    .arrow-stem {
      stroke-dasharray: 150;
      stroke-dashoffset: 150;
      animation: drawLine 2s ease-out forwards 0.5s;
    }

    .arrow-tip {
      opacity: 0;
      animation: showTip 0.1s ease-out forwards 1.7s;
      fill: var(--color-black);
    }
  }

  @keyframes drawLine {
    to {
      stroke-dashoffset: 0;
    }
  }

  @keyframes showTip {
    to {
      opacity: 1;
    }
  }

  @media (max-width: 65rem) {
    .decoration-text-arrow {
      display: none;
    }
  }
</style>

<script></script>
