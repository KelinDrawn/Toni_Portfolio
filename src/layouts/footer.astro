<footer>
  <p>© {new Date().getFullYear().toString()} Toni Schläppi</p>
</footer>

<style>
  footer {
    display: flex;
    padding-block-end: var(--space-m);
    align-items: flex-end;
    justify-content: center;
    flex-direction: row;

    background: linear-gradient(
      180deg,
      light-dark(
          color-mix(in oklch, var(--color-white) 100%, var(--color-black)),
          color-mix(in oklch, var(--color-rednight) 0%, var(--color-white))
        )
        0%,
      light-dark(
          color-mix(in oklch, var(--color-white) 96%, var(--color-black)),
          color-mix(in oklch, var(--color-rednight) 100%, var(--color-white))
        )
        100%
    );

    min-height: 15rem;
    margin-block-end: 0;
  }
</style>
